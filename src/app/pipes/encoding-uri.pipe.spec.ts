import { EncodingUriPipe } from './encoding-uri.pipe';

describe('EncodingUriPipe', () => {
  it('create an instance', () => {
    const pipe = new EncodingUriPipe();
    expect(pipe).toBeTruthy();
  });
});
